{
  "name": "haydn",
  "version": "0.0.0",
  "private": true,
  "description": "A spatial runtime for the open web",
  "workspaces": [
    "src/packages/*",
    "src/sandbox"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w @haydn/sandbox",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
